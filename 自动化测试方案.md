# 社区健康守护系统 — 自动化测试方案

> **文档版本**：v0.1  
> **最后更新**：2026-05-18

---

## 1. 测试策略总览

| 测试层级 | 框架/工具 | 覆盖目标 | 执行频率 |
|---------|----------|---------|---------|
| **单元测试** | JUnit 5 + Mockito | Service 层业务逻辑、工具类 | 每次提交 |
| **接口测试** | Spring Boot Test + MockMvc | 所有 REST API | 每次提交 |
| **集成测试** | Testcontainers | 数据库交互、Redis、消息队列 | 每日构建 |
| **端到端测试** | Selenium / Cypress | 关键用户旅程 | 发版前 |

---

## 2. 单元测试规范

### 2.1 覆盖要求

| 层级 | 覆盖率要求 | 说明 |
|------|----------|------|
| Service 层 | ≥ 80% | 核心业务逻辑必须全覆盖 |
| 工具类（Utils） | ≥ 90% | 纯函数类，成本低 |
| Controller 层 | ≥ 70% | 参数校验、响应格式 |
| Repository/Mapper | ≥1000条数据场景 | 验证 SQL 正确性和性能 |

### 2.2 命名规范

```
测试类: {目标类名}Test
测试方法: {方法名}_{场景}_{预期结果}

示例:
AlertServiceTest
  └── findByRiskLevel_whenHighRiskUser_shouldReturnRedAlerts()
  └── findByRiskLevel_whenNormalUser_shouldReturnEmptyList()
```

### 2.3 核心模块测试用例

#### 预警引擎模块

| 测试场景 | 输入 | 预期输出 |
|---------|------|---------|
| 高风险用户查询预警 | userId=高风险，riskLevel=4 | 返回红色预警列表 |
| 正常用户查询预警 | userId=正常，riskLevel=1 | 返回绿色预警或空 |
| 规则引擎匹配 | 失能老人+2月无消费 | 触发红色预警 |
| 边界条件-空数据 | 新用户无任何记录 | 返回空列表不报错 |

#### 任务管理模块

| 测试场景 | 输入 | 预期输出 |
|---------|------|---------|
| 创建访视任务 | 完整任务信息 | 任务状态=PENDING，自动指派 |
| 超时未处理 | 任务创建超7天 | 自动升级为橙色，通知上级 |
| 任务闭环 | 上传照片+填写结论 | 状态=CLOSED，留痕日志生成 |
| 并发指派 | 同一任务被两人同时领取 | 仅一人成功，另一人提示冲突 |

#### 用户服务模块

| 测试场景 | 输入 | 预期输出 |
|---------|------|---------|
| 正常注册 | 合法手机号+密码 | 注册成功，返回JWT |
| 重复注册 | 已注册手机号 | 返回409冲突 |
| 登录成功 | 正确凭证 | 返回JWT Token |
| 登录失败 | 错误密码 | 返回401，不泄露用户是否存在 |
| Token 过期 | 过期Token访问 | 返回401，前端跳转登录 |

---

## 3. 接口测试规范

### 3.1 MockMvc 测试模板

```java
@SpringBootTest
@AutoConfigureMockMvc
class AlertControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    void getAlerts_whenAuthenticated_shouldReturn200() throws Exception {
        mockMvc.perform(get("/api/alerts")
                .header("Authorization", "Bearer " + validToken)
                .param("page", "1")
                .param("size", "20"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(200))
            .andExpect(jsonPath("$.data.records").isArray());
    }

    @Test
    void getAlerts_whenUnauthenticated_shouldReturn401() throws Exception {
        mockMvc.perform(get("/api/alerts"))
            .andExpect(status().isUnauthorized());
    }
}
```

### 3.2 接口测试覆盖清单

| 模块 | 接口数量 | 测试用例数（预估） | 关键验证点 |
|------|---------|-------------------|-----------|
| 用户服务 | ~8 | 24 | JWT 鉴权、密码加密、敏感数据脱敏 |
| 预警引擎 | ~6 | 18 | 规则匹配准确性、分级预警 |
| 任务管理 | ~10 | 30 | 并发领取、超时升级、闭环留痕 |
| 健康科普 | ~5 | 15 | 关联推荐准确性 |
| 社区互动 | ~6 | 18 | 匿名问答、内容审核 |
| 监管看板 | ~5 | 15 | 数据聚合正确性、导出格式 |

---

## 4. 集成测试方案

### 4.1 数据库集成测试

```java
@Testcontainers
@SpringBootTest
class AlertRepositoryIntegrationTest {

    @Container
    static MySQLContainer<?> mysql = new MySQLContainer<>("mysql:8.0")
        .withDatabaseName("test_db")
        .withUsername("test")
        .withPassword("test");

    @DynamicPropertySource
    static void configure(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", mysql::getJdbcUrl);
        registry.add("spring.datasource.username", mysql::getUsername);
        registry.add("spring.datasource.password", mysql::getPassword);
    }
}
```

### 4.2 关键集成测试场景

| 场景 | 涉及组件 | 验证点 |
|------|---------|--------|
| 预警触发器 → 任务生成 | DB + 规则引擎 | 预警触发后自动生成对应级别任务 |
| 任务闭环 → 日志写入 | DB + 文件存储 | 拍照上传后日志完整、可追溯 |
| Redis 缓存一致性 | DB + Redis | 数据更新后缓存同步失效 |
| Seata 分布式事务 | 多服务 + DB | 事务失败时数据回滚完整 |

---

## 5. 性能测试基准

| 接口类型 | 目标 QPS | 响应时间 P99 | 并发数 |
|---------|---------|-------------|--------|
| 查询类（列表/详情） | ≥ 500 | ≤ 200ms | 100 |
| 写入类（创建/更新） | ≥ 200 | ≤ 500ms | 50 |
| 聚合类（看板数据） | ≥ 100 | ≤ 2s | 30 |

---

## 6. CI/CD 集成

```yaml
# .github/workflows/test.yml
name: Test

on: [push, pull_request]

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Run Unit Tests
        run: mvn test -pl '!integration-tests'
      - name: Generate Coverage Report
        run: mvn jacoco:report

  integration-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Integration Tests
        run: mvn verify -pl integration-tests -Pintegration
```

---

## 7. 测试数据管理

| 策略 | 说明 |
|------|------|
| **测试数据隔离** | 每个测试类使用独立的测试数据集，通过 `@Sql` 注解初始化 |
| **数据清理** | 使用 `@Transactional` 自动回滚，或 `@AfterEach` 手动清理 |
| **敏感数据** | 测试数据中不包含真实用户信息，使用随机生成器 |