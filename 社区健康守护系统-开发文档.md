# 社区健康守护系统 — 开发文档（初稿）

> **副标题**：一个以「预防为主、身心共护」为核心理念的社区智慧健康生态  
> **文档版本**：v0.1（初稿）  
> **最后更新**：2026-05-18

---

## 目录

1. [项目概述](#1-项目概述)
2. [项目愿景](#2-项目愿景)
3. [用户角色定义](#3-用户角色定义)
4. [核心功能模块](#4-核心功能模块)
   - 4.1 [健康风险感知与预警引擎](#41-健康风险感知与预警引擎)
   - 4.2 [多端协同与任务管理](#42-多端协同与任务管理)
   - 4.3 [健康教育与人本体验](#43-健康教育与人本体验)
   - 4.4 [可持续商业模式](#44-可持续商业模式)
5. [技术架构](#5-技术架构)
   - 5.1 [整体架构](#51-整体架构)
   - 5.2 [关键技术选型](#52-关键技术选型)
   - 5.3 [Nacos 统一配置管理方案](#53-nacos-统一配置管理方案)
   - 5.4 [权限模型与数据库规范](#54-权限模型与数据库规范)
   - 5.5 [前端通信方案](#55-前端通信方案)
   - 5.6 [数据可视化方案](#56-数据可视化方案)
   - 5.7 [代码规范与性能优化](#57-代码规范与性能优化)
6. [社会影响力推演](#6-社会影响力推演)
7. [面试叙事框架](#7-面试叙事框架)
8. [附录](#8-附录)

---

## 1. 项目概述

### 1.1 项目定位

社区健康守护系统是一套面向社区场景的智慧健康管理平台，以「预防为主、身心共护」为核心理念，通过数据驱动的风险感知、多端协同的任务管理，以及沉浸式的健康教育体验，帮助全年龄段社区居民主动管理自身健康，同时为社区服务者和监管方提供高效的工具支撑。

### 1.2 核心问题

当前社会存在一个恶性循环：人们拼命赚钱 → 忽视健康 → 患病后花光积蓄治疗 → 康复后不得不更加拼命赚钱。本系统旨在通过技术手段打破这一循环，让人们「看见」未来的健康风险，主动为自己的健康负责。

### 1.3 项目价值主张

- **对个人**：提前感知健康风险，获得个性化的预防方案和行动清单
- **对社区**：提升服务效率，实现任务闭环管理，工作留痕可追溯
- **对社会**：降低医保基金压力，推动「对自己健康负责」成为社会共识

---

## 2. 项目愿景

打破「拿命换钱，再用钱换命」的恶性循环。通过技术手段，让人们看见未来的健康风险，主动为自己的健康负责，最终构建一个更健康、更公平、更有尊严的社会。

---

## 3. 用户角色定义

| 角色 | 用户群体 | 核心需求 |
|------|---------|---------|
| **服务对象** | 全年龄段社区居民（含失能/半失能人群、慢病患者、亚健康上班族、普通居民） | 被系统主动发现并守护，获得风险预警、健康科普、心理支持 |
| **社区服务者** | 社区网格员、志愿者、社区医生 | 任务管理、代办导航、工作留痕 |
| **监管方** | 社区管理者、残联、民政部门 | 数据看板、资金流向监管、资源调度决策 |
| **科普体验者** | 任何有健康风险意识的用户 | 通过模拟体验，看清未来风险，获得行动清单 |

---

## 4. 核心功能模块

### 4.1 健康风险感知与预警引擎

> **系统的心脏，只做三件事：感知风险、发起干预、闭环追踪。**

#### 4.1.1 四级预警与干预体系

| 预警级别 | 触发条件 | 系统动作 |
|---------|---------|---------|
| **一级（绿色）** | 健康档案出现高风险因素（独居、慢病、残疾未评级） | 列入「重点关注名单」，每月推送常规访视提醒 |
| **二级（黄色）** | 体检数据异常，或家属反馈其自理能力下降 | 生成「健康干预任务」，指派社区医生7天内上门复核 |
| **三级（橙色）** | 社区医生诊断「有失能风险」 | 启动「预失能关怀流程」：推康复计划、辅助器具信息、临时救助申请指引 |
| **四级（红色）** | 已评级为残疾，但补贴数据连续2个月无消费/无访视记录 | 生成「高危失联预警」，要求24小时内入户核实，结果拍照上传闭环 |

#### 4.1.2 规则引擎配置化

不同人群对应不同的「风险规则表」：

| 人群类型 | 监测指标 | 预警规则示例 |
|---------|---------|------------|
| 失能老人 | 补贴消费记录、访视记录 | 连续2个月无消费记录 → 红色预警 |
| 慢病患者 | 血压/血糖数据 | 连续3天超过阈值 → 黄色预警 |
| 普通用户 | 可穿戴设备数据 | 静息心率持续异常 → 绿色预警 |

**技术要点**：规则引擎采用配置化设计，支持动态加载规则集，无需修改代码即可调整预警策略。

---

### 4.2 多端协同与任务管理

#### 4.2.1 服务对象端

| 功能点 | 描述 | 备注 |
|-------|------|------|
| 无障碍求助 | 支持物联网按钮、智能音箱接入 | 降低使用门槛 |
| 社区主动探访 | 系统自动生成探访任务 | 被动服务转主动关怀 |
| 工单追踪 | 查看求助工单处理进度 | 透明化服务流程 |

> **模拟实现方案**：后台自动生成求助工单，推送至社区服务端。

#### 4.2.2 社区服务端

| 功能点 | 描述 | 备注 |
|-------|------|------|
| 代办流程导航 | 补贴申领、代填表格、消费留痕（拍照上传） | 标准化操作指引 |
| 访视任务管理 | 任务列表、状态追踪、超时自动升级报警 | 确保服务闭环 |
| 工作留痕 | 操作日志自动记录 | 支撑监管审计 |

#### 4.2.3 监管端

| 功能点 | 描述 | 备注 |
|-------|------|------|
| 数据看板 | 补贴资金流向、服务数据总览、预警处理效率 | 可视化呈现 |
| 报表导出 | 支持按周期、按维度导出报表 | 支撑政府/社区决策 |
| 资源调度 | 基于数据驱动的资源分配建议 | 优化服务效率 |

---

### 4.3 健康教育与人本体验

#### 4.3.1 健康科普模块

| 功能点 | 描述 |
|-------|------|
| 关联式推送 | 根据用户健康数据，精准推送相关科普内容 |
| 可视化解读 | 用动画/图表解释复杂医学术语 |
| 正向叙事 | 聚焦「现在开始，永远不晚」，而非恐吓式宣传 |

#### 4.3.2 「对号入座」职业病风险引导

1. 用户选择职业身份：久坐办公族、体力劳动者、轮班族等
2. 系统自动匹配高发职业病清单
3. 链接到「疾病体验模块」，引导深度体验

#### 4.3.3 疾病体验与决策模拟模块（核心亮点）

| 体验方式 | 实现手段 | 目的 |
|---------|---------|------|
| **视觉** | AI 生成第一人称视频，模拟疾病发作（视野缺损、偏瘫无力） | 让人「看见」未来 |
| **听觉** | 模拟 ICU 环境声、家属讨论病情声 | 制造沉浸感 |
| **触觉/限制** | 单手穿衣挑战、吸管呼吸、手机震动模拟慢性疼痛 | 身体感知疾病 |
| **决策模拟** | 体验后自动生成「未来10年医疗费用预估账单」，并对比「这笔钱可以换成的美好生活」 | 经济冲击，形成行动动机 |

#### 4.3.4 社区分享与心理支持模块

| 功能点 | 描述 |
|-------|------|
| 分话题社区 | 高血压、术后康复、抗焦虑等频道 |
| 故事广场 | 康复历程分享，形成正向互助氛围 |
| 匿名问答 | 由社区医生、志愿者、其他用户回答，降低心理门槛 |
| 每日正向话题 | 例如「今天有什么事让你感觉稍微好一点？」 |

---

### 4.4 可持续商业模式

> **核心原则**：不依赖国家兜底，自我造血，覆盖成本，持续服务。

| 收入来源 | 服务对象 | 产品/服务 | 定价逻辑 |
|---------|---------|---------|---------|
| **B端**（稳定收入） | 企业HR、保险公司 | 脱敏后的「员工/客户健康风险管理报告」 | 年费制，为企业提供健康风险分析和干预方案 |
| **C端**（筛选诚意） | 个人用户 | 深度疾病体验服务 | 按职业风险阶梯定价（¥9–199），覆盖设备维护和场地成本 |
| **G端**（普惠兜底） | 政府/社区 | 购买服务，为弱势群体免费提供服务 | 向政府证明系统节约医保基金的价值，换取服务采购 |

---

## 5. 技术架构

### 5.1 整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                         前端层（多端）                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │ 服务对象端 │  │ 社区服务端 │  │  监管端   │                   │
│  │ (小程序)  │  │ (小程序)  │  │ (Web)   │                   │
│  │ Vue3 + TS │  │ Vue3 + TS │  │ Vue3+TS │                   │
│  └──────────┘  └──────────┘  └──────────┘                   │
├──────────────────────────────────────────────────────────────┤
│                    网关层 (Gateway)                            │
│               Spring Cloud Gateway + JWT 鉴权                  │
├──────────────────────────────────────────────────────────────┤
│                     业务服务层（微服务）                          │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐     │
│  │预警引擎 │ │任务管理 │ │健康科普 │ │用户服务 │ │社区互动 │     │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘     │
│  ┌────────┐ ┌────────┐                                       │
│  │数据分析 │ │规则引擎 │                                       │
│  └────────┘ └────────┘                                       │
├──────────────────────────────────────────────────────────────┤
│                       数据层                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │  MySQL   │  │  Redis   │  │   OSS    │                   │
│  │(utf8mb4) │  │ (缓存)   │  │(文件存储) │                   │
│  │3306端口  │  │          │  │          │                   │
│  └──────────┘  └──────────┘  └──────────┘                   │
├──────────────────────────────────────────────────────────────┤
│                   基础设施层（微服务治理）                         │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│  │    Nacos     │ │   Sentinel   │ │    Seata     │          │
│  │ 注册中心     │ │  流量防护    │ │  分布式事务   │          │
│  │ + 配置中心   │ │  规则持久化  │ │  事务分组    │          │
│  │ (Seata/Sentinel/│  → Nacos   │ │  → Nacos    │          │
│  │  YAML 统一管理)  │ │             │ │              │          │
│  └──────────────┘ └──────────────┘ └──────────────┘          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │ IoT 网关  │  │ AI 服务   │  │ 消息队列  │                   │
│  │(设备接入) │  │(内容生成) │  │(异步任务) │                   │
│  └──────────┘  └──────────┘  └──────────┘                   │
└──────────────────────────────────────────────────────────────┘
```

### 5.2 关键技术选型

| 层级 | 技术选型 | 版本/说明 |
|------|---------|----------|
| **前端框架** | Vue 3 + Element Plus | TypeScript 编写脚本，SCSS 编写样式 |
| **后端框架** | Spring Boot 2.7+ / Spring Cloud | 微服务模块化，Spring Cloud Gateway 做网关 |
| **ORM** | MyBatis Plus | 简化 CRUD，分页插件 |
| **安全认证** | Spring Security + JWT | 无状态鉴权，敏感数据加密存储 |
| **数据库** | MySQL 8.0 | utf8mb4 字符集，Asia/Shanghai 时区，3306 端口，SSL 强制连接 |
| **缓存** | Redis | 高频读写数据缓存，分布式锁防并发 |
| **注册中心 & 配置中心** | Nacos | ✅ **必须使用** — Seata 配置、Sentinel 规则、各服务 YAML 统一存放在 Nacos 中 |
| **流量防护** | Sentinel | 规则持久化至 Nacos，统一管理，方便测试 |
| **分布式事务** | Seata | AT 模式，事务分组配置存储在 Nacos |
| **文件存储** | 阿里云 OSS / MinIO | 拍照上传留痕 |
| **AI 服务** | 大模型 API | 疾病体验模块的视频/文本内容生成 |
| **IoT 接入** | MQTT 协议 + 设备网关 | 物联网按钮、智能音箱接入 |
| **规则引擎** | 自研轻量规则引擎 | 预警规则配置化，支持动态加载 |

### 5.3 Nacos 统一配置管理方案

> **核心原则：所有配置必须纳入 Nacos，不允许散落在本地 application.yml 中。**

Nacos 在本系统中承担双重角色：**服务注册发现** + **统一配置中心**。所有微服务模块的 YAML 配置、Seata 事务配置、Sentinel 流控/熔断规则，全部通过 Nacos 进行集中管理。

#### 5.3.1 Nacos 命名空间规划

| 命名空间 ID | 环境 | 用途 |
|------------|------|------|
| `community-health-dev` | 开发环境 | 本地开发调试，日志级别 DEBUG |
| `community-health-test` | 测试环境 | 集成测试、QA 验证，Sentinel 规则可动态调整 |
| `community-health-prod` | 生产环境 | 正式环境，配置变更需审批 |

#### 5.3.2 Nacos 配置清单

```
Nacos 配置中心 (community-health-dev)
├── Data ID: community-health-gateway.yaml          ← 网关路由规则
├── Data ID: community-health-user-service.yaml     ← 用户服务数据源/Redis/MyBatis
├── Data ID: community-health-alert-engine.yaml     ← 预警引擎服务
├── Data ID: community-health-task-service.yaml     ← 任务管理服务
├── Data ID: seata-server.properties                ← Seata 服务端配置
├── Data ID: community-health-seata-client.yaml     ← 各微服务 Seata 客户端配置
├── Data ID: community-health-sentinel-rules.json   ← Sentinel 流控/熔断/热点规则
└── Data ID: community-health-common.yaml           ← 全局公共配置（线程池、日志等）
```

#### 5.3.3 各微服务 bootstrap.yml 统一引用 Nacos

```yaml
# bootstrap.yml — 每个微服务模块统一模板
spring:
  application:
    name: community-health-xxx-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: ${NACOS_NAMESPACE:community-health-dev}
      config:
        server-addr: 127.0.0.1:8848
        namespace: ${NACOS_NAMESPACE:community-health-dev}
        file-extension: yaml
        shared-configs:
          - data-id: community-health-common.yaml
            group: DEFAULT_GROUP
            refresh: true
```

#### 5.3.4 Seata 配置托管至 Nacos

Seata 的事务分组（`tx-service-group`）与 TC 集群映射关系配置在 Nacos 中，实现服务端和客户端统一管理：

**Nacos 中创建的 Seata 配置项：**

| Data ID | Group | 内容示例 |
|---------|-------|---------|
| `service.vgroupMapping.community-health-tx-group` | `SEATA_GROUP` | `default` |
| `store.mode` | `SEATA_GROUP` | `db` |
| `store.db.datasource` | `SEATA_GROUP` | `druid` |
| `store.db.dbType` | `SEATA_GROUP` | `mysql` |
| `store.db.url` | `SEATA_GROUP` | `jdbc:mysql://127.0.0.1:3306/seata?...` |

**微服务端 seata 客户端配置（Nacos 中统一管理）：**

```yaml
# community-health-seata-client.yaml (Nacos)
seata:
  enabled: true
  tx-service-group: community-health-tx-group
  service:
    vgroup-mapping:
      community-health-tx-group: default
  registry:
    type: nacos
    nacos:
      server-addr: 127.0.0.1:8848
      namespace: ${NACOS_NAMESPACE}
      group: SEATA_GROUP
  config:
    type: nacos
    nacos:
      server-addr: 127.0.0.1:8848
      namespace: ${NACOS_NAMESPACE}
      group: SEATA_GROUP
```

#### 5.3.5 Sentinel 规则持久化至 Nacos

Sentinel 默认将流控规则存储在内存中，服务重启即丢失。通过配置持久化至 Nacos，可实现规则统一管理、环境隔离、测试复用。

**微服务端 Sentinel 持久化配置（在每个微服务的 Nacos YAML 中）：**

```yaml
spring:
  cloud:
    sentinel:
      transport:
        dashboard: 127.0.0.1:8080
      datasource:
        flow:
          nacos:
            server-addr: 127.0.0.1:8848
            namespace: ${NACOS_NAMESPACE}
            group-id: SENTINEL_GROUP
            data-id: ${spring.application.name}-flow-rules
            rule-type: flow
        degrade:
          nacos:
            server-addr: 127.0.0.1:8848
            namespace: ${NACOS_NAMESPACE}
            group-id: SENTINEL_GROUP
            data-id: ${spring.application.name}-degrade-rules
            rule-type: degrade
        param-flow:
          nacos:
            server-addr: 127.0.0.1:8848
            namespace: ${NACOS_NAMESPACE}
            group-id: SENTINEL_GROUP
            data-id: ${spring.application.name}-param-rules
            rule-type: param-flow
```

**Nacos 中 Sentinel 规则 JSON 示例（`community-health-user-service-flow-rules`）：**

```json
[
  {
    "resource": "/api/user/login",
    "limitApp": "default",
    "grade": 1,
    "count": 100,
    "strategy": 0,
    "controlBehavior": 0,
    "clusterMode": false
  },
  {
    "resource": "/api/user/register",
    "limitApp": "default",
    "grade": 1,
    "count": 50,
    "strategy": 0,
    "controlBehavior": 0,
    "clusterMode": false
  }
]
```

### 5.4 权限模型与数据库规范

#### 5.4.1 RBAC 权限模型

采用 RBAC（基于角色的访问控制），权限按模块拆分，角色与模块权限灵活绑定。

| 角色 | 编码 | 权限范围 |
|------|------|---------|
| **超级管理员** | `SUPER_ADMIN` | 全系统权限，可管理所有角色和用户 |
| **普通管理员** | `ADMIN` | 仅管理所辖范围数据，如某个社区的数据看板和任务 |
| **普通用户** | `USER` | 服务对象端基础功能：查看预警、接收科普、参与社区 |

**模块权限拆分示例：**

```
模块: 预警管理    → 权限: alert:view, alert:edit, alert:resolve
模块: 任务管理    → 权限: task:view, task:assign, task:close
模块: 监管看板    → 权限: dashboard:view, dashboard:export
模块: 用户管理    → 权限: user:view, user:edit, user:delete
模块: 系统配置    → 权限: system:config:view, system:config:edit
```

#### 5.4.2 数据库与并发控制

| 维度 | 规范 |
|------|------|
| **数据库** | MySQL 8.0，3306 端口 |
| **字符集** | utf8mb4 |
| **时区** | `Asia/Shanghai` |
| **连接加密** | SSL 强制连接 |
| **并发控制** | 数据库层加悲观锁/乐观锁防止脏读脏写；代码层额外做业务判断，双重保障 |

#### 5.4.3 开发顺序

1. **搭框架** → 前后端基础脚手架、Nacos + Sentinel + Seata 基础设施就位
2. **跑通链路** → 先开发用户端（USER 角色），完成注册/登录/预警查看/社区互动核心闭环
3. **权限配置** → 接入 Spring Security + JWT，完成 `USER` 和 `ADMIN` 角色权限
4. **细化迭代** → 补充业务细节、优化体验、丰富监管端看板

### 5.5 前端通信方案

前端统一使用 **Axios** 与后端进行 HTTP 通信，封装统一的请求/响应拦截器，确保每一次请求都经过认证、错误处理和日志记录。

#### 5.5.1 Axios 实例封装

```typescript
// src/utils/request.ts
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { ElMessage } from 'element-plus'

const service: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 15000,
  headers: { 'Content-Type': 'application/json' }
})

service.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const token = localStorage.getItem('token')
    if (token && config.headers) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => Promise.reject(error)
)

service.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, message, data } = response.data
    if (code === 200) return data
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    ElMessage.error(error.message || '网络异常')
    return Promise.reject(error)
  }
)

export default service
```

#### 5.5.2 API 接口组织

按模块拆分 API 文件，与后端微服务一一对应：

```
src/api/
├── user.ts          ← 用户服务：注册、登录、个人信息
├── alert.ts         ← 预警引擎：风险列表、预警详情
├── task.ts          ← 任务管理：访视任务、代办工单
├── health.ts        ← 健康科普：科普文章、疾病体验
├── community.ts     ← 社区互动：话题、故事广场
└── dashboard.ts     ← 监管看板：数据统计、报表导出
```

### 5.6 数据可视化方案

所有状态数据统一使用 **ECharts** 进行可视化呈现，覆盖管理员监管看板和用户个人健康概览两大类场景。

#### 5.6.1 监管端看板（管理员视角）

| 图表类型 | 数据内容 | 交互 |
|---------|---------|------|
| 折线图 | 各社区月度预警触发趋势 | 支持按预警级别筛选、时间段缩放 |
| 饼图 | 预警级别分布（绿/黄/橙/红占比） | 点击扇区下钻到明细列表 |
| 柱状图 | 各社区服务完成率对比 | 横向对比，悬停显示具体数值 |
| 仪表盘 | 当月预警闭环率 | 实时刷新，目标线 95% |
| 地图 | 社区风险热力分布 | 按区域着色，点击进入社区详情 |

#### 5.6.2 个人健康概览（用户视角）

| 图表类型 | 数据内容 | 交互 |
|---------|---------|------|
| 折线图 | 近 30 天血压/血糖/心率趋势 | 可切换指标 |
| 环形图 | 健康任务完成度 | 显示「已完成/待完成」比例 |
| 雷达图 | 六维健康评分（运动、饮食、睡眠、心理、慢病、体态） | 每项可点击查看改善建议 |
| 时间轴 | 预警历史与干预记录 | 按时间倒序，颜色标识预警等级 |

#### 5.6.3 ECharts 封装示例

```typescript
// src/composables/useChart.ts
import * as echarts from 'echarts'
import { onMounted, onUnmounted, ref, Ref } from 'vue'

export function useChart(chartRef: Ref<HTMLElement | null>) {
  let chartInstance: echarts.ECharts | null = null

  const initChart = () => {
    if (!chartRef.value) return
    chartInstance = echarts.init(chartRef.value)
  }

  const setOption = (option: echarts.EChartsOption) => {
    chartInstance?.setOption(option, true)
  }

  const resize = () => chartInstance?.resize()

  onMounted(() => {
    initChart()
    window.addEventListener('resize', resize)
  })

  onUnmounted(() => {
    window.removeEventListener('resize', resize)
    chartInstance?.dispose()
  })

  return { setOption }
}
```

### 5.7 代码规范与性能优化

#### 5.7.1 后端代码规范

**优先使用流式编程（Stream API / Lambda）**：

```java
// ✅ 推荐：流式编程
List<AlertDTO> alerts = alertList.stream()
    .filter(a -> a.getLevel() >= userRiskLevel)
    .sorted(Comparator.comparing(Alert::getCreatedAt).reversed())
    .map(AlertConverter::toDTO)
    .collect(Collectors.toList());

// ❌ 避免：传统 for 循环嵌套
List<AlertDTO> result = new ArrayList<>();
for (Alert a : alertList) {
    if (a.getLevel() >= threshold) {
        result.add(AlertConverter.toDTO(a));
    }
}
Collections.sort(result, (a, b) -> b.getCreatedAt().compareTo(a.getCreatedAt()));
```

| 规范项 | 要求 |
|--------|------|
| **流式编程** | 集合操作优先使用 Stream API + Lambda + 方法引用 |
| **命名规范** | Service 层方法语义化（`findByRiskLevel` 而非 `getList`），DTO/VO 按用途命名 |
| **异常处理** | 全局异常拦截器统一处理，业务层只抛业务异常，禁止吞异常 |
| **日志规范** | 关键操作记录 INFO 日志（含入参和耗时），异常记录 ERROR + 堆栈 |

#### 5.7.2 SQL 优化规范

| 优化方向 | 具体措施 |
|---------|---------|
| **索引设计** | 高频查询字段建立联合索引，避免冗余索引，定期分析慢查询日志 |
| **避免 N+1** | 使用 MyBatis Plus 的 `batchSelect` 批量查询，关联查询用 JOIN 而非循环 |
| **分页查询** | 所有列表接口必须分页，MyBatis Plus 分页插件自动 COUNT 优化 |
| **Explain 审查** | 开发阶段对所有新增 SQL 执行 `EXPLAIN`，确保无全表扫描、无文件排序 |
| **连接池** | HikariCP 连接池，最小空闲 10，最大 50，连接超时 30s |

#### 5.7.3 接口与算法优化

| 优化方向 | 具体措施 |
|---------|---------|
| **缓存策略** | 高频只读数据（社区列表、字典项）用 Redis 缓存，TTL 30 分钟 |
| **异步处理** | 预警通知推送、报表生成等耗时操作走消息队列异步执行 |
| **批量操作** | 批量预警规则匹配用一次查询替代逐条判断，减少 DB 交互 |
| **算法复杂度** | 规则引擎匹配优先用哈希索引，避免 O(n²) 遍历；数据聚合优先 SQL 层完成 |
| **接口合并** | 前端需要多模块数据时，后端提供聚合接口而非前端多次请求 |

#### 5.7.4 前端性能优化

| 优化方向 | 具体措施 |
|---------|---------|
| **首屏加载** | 路由懒加载、ECharts 按需引入、图片懒加载 |
| **请求优化** | 并发请求用 `Promise.all`，非关键数据延迟加载 |
| **状态管理** | Pinia 模块化管理，避免全局状态膨胀 |
| **交互反馈** | 加载态用骨架屏，操作结果即时 Toast 反馈，防止重复提交 |

---

## 6. 社会影响力推演

```
你的系统（认知疫苗）
    ↓
人们亲眼看见未来健康风险
    ↓
认知转变：健康成为第一优先级
    ↓
行为改变：主动投保、主动预防、主动就医
    ↓
系统减负：重病晚期患者减少，医保基金压力降低
    ↓
红利释放：医保有余力兜底，个人有余钱消费，企业不再压榨人力
    ↓
文化重塑：「带病坚持」不再是美德，「对自己健康负责」成为共识
```

---

## 7. 面试叙事框架

> 以下为面试场景中的推荐讲述结构，可根据面试岗位和时长灵活裁剪。

### 7.1 开场（问题定义）

> 「我看到一个恶性循环：人们拼命赚钱，是为了有一天生病时能付得起医药费。但如果能少生病，根本不需要赚那么多钱来对抗恐惧。」

### 7.2 我的解法（系统简介）

> 「我设计了一套『社区健康守护系统』。它只做一件事：让人们亲眼看见自己未来的健康风险，并给他一套今天就能开始的行动清单。」

### 7.3 核心亮点（挑一个讲）

> 「比如我的『疾病体验与决策模拟』模块。用户通过视觉、听觉、触觉的模拟，安全地提前感受疾病；然后系统生成一份未来账单；最后立刻提供一个积极的出口。这不是恐吓，是让人把对疾病的恐惧，转化为对健康的行动。」

### 7.4 可持续性（商业思维）

> 「我设计了三方共担的商业模式：企业购买员工健康报告，个人支付小额体验费，政府购买服务为弱势群体兜底。这个系统能自己养活自己，持续运转。」

### 7.5 收尾（个人价值）

> 「通过这个项目，我不仅锻炼了技术能力，更证明了：我是一个能用技术发现社会痛点，并提出系统性解决方案的人。」

---

## 8. 附录

### 8.1 术语表

| 术语 | 说明 |
|------|------|
| 规则引擎 | 可配置的预警规则系统，支持根据不同人群动态调整预警策略 |
| 闭环追踪 | 从预警触发到干预执行再到结果确认的完整流程 |
| 预失能关怀 | 对尚未失能但有失能风险的人群进行提前干预 |
| 高危失联 | 连续无消费记录或访视记录的高风险人群状态 |

### 8.2 后续工作

- [ ] 细化数据库表结构设计
- [ ] 完成各模块接口设计（API 文档）
- [ ] 规则引擎配置表详细设计
- [ ] 疾病体验模块技术验证（AI 视频生成可行性）
- [ ] 开发 MVP 版本，覆盖一级和四级预警

---

> 本文档为项目初稿，后续将根据技术验证和用户反馈持续迭代。  
> 这个项目不是一份课程作业，而是一个技术人对世界的系统思考。  
> 它证明了你不仅能写代码，更能定义问题、设计架构、洞察人性、构建可持续的商业模式。